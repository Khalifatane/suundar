/**
 * Storefront Auth Context
 *
 * Re-exports from @siggistore/auth/react for shared authentication.
 * The shared AuthProvider manages auth state across both storefront and admin.
 */
export { AuthProvider, useAuth, useAuthButton } from '@siggistore/auth/react';
