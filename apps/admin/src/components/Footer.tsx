export function Footer() {
  return (
    <footer className="footer-bar">
      <strong>Runtime Overview</strong>
      <div className="muted">
        The HTML-driven admin remains intact while the SPA layer grows beside it.
      </div>
    </footer>
  );
}
